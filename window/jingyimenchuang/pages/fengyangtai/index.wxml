<view>
  <view class='herder-title'>封阳台案例</view>
  <view wx:for="{{imgaList}}" wx:for-item="image" class="previewimg">
    <!-- <view class='bottom_line'></view> -->
		<view class="product_list_block">
      <image src="{{image.url}}" data-src="{{image.url}}" bindtap="previewImage" class='commomn_image' mode="widthFix"></image>
    </view>
    	<view class="product_list_block txtcontent">
        {{image.title}}
      </view>
	</view>
  <button type="primary" class="submitBtn" id="submitBtn" bindtap="callPhone">咨询客服</button>
</view>